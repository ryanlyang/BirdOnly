schema_version: 1
phase: background_sanitized

phase0_dir: /home/ryreu/guided_cnn/logsWaterbird/setv_waterbirds95/phase0
mask_bank_dir: null
output_root: /home/ryreu/guided_cnn/logsWaterbird/setv_waterbirds95/background_sanitized

model:
  architecture: vit_small_patch16_224
  pretrained: true
  num_classes: 2
  normalization_mean: [0.5, 0.5, 0.5]
  normalization_std: [0.5, 0.5, 0.5]

input:
  green_rgb: [0, 255, 0]
  training_masks_per_image: 2
  validation_masks_per_image: 8
  geometric_augmentation: canonical_only

training:
  seed: null
  epochs: 20
  optimizer: AdamW
  learning_rate: 0.00003
  weight_decay: 0.05
  batch_size: 32
  evaluation_batch_size: 16
  scheduler: cosine
  warmup_epochs: 2
  label_smoothing: 0.0
  lambda_consistency: 0.5
  mixed_precision: true
  num_workers: 8
  device: cuda

storage:
  save_final_checkpoint: true
  save_intermediate_checkpoints: false
  checkpoint_filename: background_sanitized_final.pt
  scores_filename: background_sanitized_val_scores.npz
